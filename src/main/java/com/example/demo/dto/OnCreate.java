package com.example.demo.dto;

/**
 * Validation group applied only when creating a new resource.
 * Used to enforce constraints (e.g. mandatory password) that should
 * not apply to partial updates.
 */
public interface OnCreate {
}
